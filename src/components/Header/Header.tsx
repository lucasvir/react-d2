import { Center, Heading } from "@chakra-ui/react";

export const Header = () => {
    return (
        <header>
            <Center height="60px">
                <Heading>Dio Bank</Heading>
            </Center>
        </header>
    );
};
